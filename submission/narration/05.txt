On the next turn, the model increments its document and token counts with my confirmed examples. Predictions are recalculated immediately. Diagnostics expose one-versus-rest token weights, aggregate confusion counts, confidence calibration, log loss, and per-class scores without returning individual holdout answers.
