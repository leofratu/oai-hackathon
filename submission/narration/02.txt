Chrome exposes WebMCP on this deployed page, and the green badge confirms seven tools registered through document model context. The browser agent receives typed operations for model state, uncertainty sampling, label requests, training, evaluation, settings, and history.
