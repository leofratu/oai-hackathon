Chrome exposes WebMCP on this deployed page, and the green badge confirms nine tools registered through document model context. The browser agent receives typed operations for training, evaluation, learned-model diagnostics, and direct inference. It can query the model without interpreting the page layout.
